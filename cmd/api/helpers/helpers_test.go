package helpers

import (
	"github.com/stretchr/testify/assert"
	"testing"
)

func TestTransformExchangeApiResponseDataToRateModel(t *testing.T) {
	t.Run("successfully transforms data from exchange api response to rate model", func(t *testing.T) {
		assert.Equal(t, true, true)
	})
}
